# Agent 学习与机票比价项目计划

## 1. 计划目标

本计划同时服务两个目标：

1. 系统学习 AI Agent 的核心知识，包括结构化输出、工具调用、Agent Loop、工作流编排、MCP、评测和生产可靠性。
2. 完成一个可运行、可评测、可部署、可用于简历展示的机票比价 Agent。

最终作品组合：

- 一个完整的机票比价 Agent。
- 一个独立的航班搜索 MCP Server。
- 一套公开、可复现的 Agent 评测结果。
- 完整的 README、架构图、演示视频和在线演示地址。

## 2. 核心原则

- 现在建立项目，但从 Stage 3 开始开发核心 Agent 功能。
- 不等待全部课程学完，学习内容应立即应用到同一个项目。
- 第一版优先使用确定性工作流，不为了展示概念强行拆分多个 Agent。
- LLM 负责理解需求、调用工具和解释结果。
- 价格计算、币种换算、去重、过滤和排序由普通代码完成。
- 每个指标必须真实测量，不在简历中编造数据。

## 3. 总体学习路线

```text
Stage 0-2：基础准备
    ↓
Stage 3：启动机票 Agent MVP
    ↓
Stage 4：引入 Agent 框架，形成完整工作流
    ↓
Stage 5：增加 MCP 项目
    ↓
Stage 6：按需学习 Memory/RAG
    ↓
Stage 7：评测、追踪、可靠性和部署
    ↓
Stage 8：可选浏览器能力与最终展示
```

## 4. 12 周计划表

| 时间 | 教程内容 | 机票项目产出 | 完成标志 |
|---|---|---|---|
| 第 1 周 | Stage 0：Python、Git、API、JSON | 建立仓库、需求文档、航班数据模型 | 能读取模拟航班 JSON，并通过测试 |
| 第 2 周 | Stage 1：LLM API、Token、模型选择 | 调用模型并返回结构化 JSON | 响应能通过 Pydantic 校验 |
| 第 3 周 | Stage 2：Prompt、Few-shot、结构化输出 | 提取城市、日期、人数、行李和偏好 | 至少 20 个需求样例测试通过 |
| 第 4-5 周 | Stage 3：Tool Calling、ReAct、Agent Loop | 完成使用模拟数据的可运行 MVP | Agent 能追问缺失参数并调用搜索工具 |
| 第 6-7 周 | Stage 4：Agent Framework | 用 LangGraph 整理搜索、追问、排序和验价流程 | 完成多步骤工作流及失败恢复测试 |
| 第 8 周 | Stage 5：选学 MCP、Skills | 实现独立的航班搜索 MCP Server | Codex 或其他 MCP 客户端可成功调用 |
| 第 9 周 | Stage 6：选学 Memory/RAG | 按需实现用户偏好记忆 | 能保存和应用用户明确确认的偏好 |
| 第 10-11 周 | Stage 7：Eval、Tracing、Production | 评测集、重试、超时、日志和成本统计 | 至少 30-50 个场景可重复评测 |
| 第 12 周 | Stage 8 选学及项目包装 | Web 演示、部署、README、架构图和演示视频 | 他人可按照文档启动并体验项目 |

> 如果已经掌握 Python、Git 和 REST API，可将第 1 周压缩为两天，并把总周期缩短到 10 周。

## 5. 项目启动时间

### 现在：项目立项

现在建立项目仓库和基础目录，但暂时不实现复杂的 Agent 框架。

建议目录：

```text
flight-agent/
├── README.md
├── docs/
│   ├── requirements.md
│   └── architecture.md
├── src/
├── tests/
├── data/
│   └── mock_flights.json
└── .env.example
```

第一版目标场景：

> 查找 9 月从上海去东京、旅行 5 天、含 20kg 行李、避免红眼航班的综合最优机票。

本阶段任务：

- [ ] 创建 Git 仓库。
- [ ] 编写项目目标和非目标。
- [ ] 定义用户输入和预期输出。
- [ ] 设计航班、航段、价格和行李数据结构。
- [ ] 准备可重复使用的模拟航班数据。
- [ ] 建立基础测试环境。

### Stage 2 完成后：开发需求解析器

将自然语言转换为结构化搜索条件，例如：

```json
{
  "origin": "SHA",
  "destination": "TYO",
  "departure_date": null,
  "trip_duration_days": 5,
  "date_flexibility_days": 2,
  "checked_baggage_kg": 20,
  "avoid_red_eye": true
}
```

要求：

- [ ] 缺失信息使用 `null`，不允许模型猜测。
- [ ] 城市和机场代码能够区分。
- [ ] 日期表达能够标准化。
- [ ] Pydantic 或等效 Schema 校验失败时能够重试。
- [ ] 建立至少 20 条需求解析测试样例。

### Stage 3：正式开发 MVP

这是机票比价 Agent 核心功能的正式启动点。

MVP 功能：

- [ ] 解析用户需求。
- [ ] 识别缺失的必要参数。
- [ ] 主动追问缺失信息。
- [ ] 调用 `search_flights` 工具。
- [ ] 从模拟数据中搜索航班。
- [ ] 计算包含行李和费用的总价。
- [ ] 输出最便宜、综合最优、最舒适三个结果。
- [ ] 显示报价来源和查询时间。
- [ ] 对无结果和工具失败给出明确反馈。

Stage 3 结束时，项目必须能够进行完整的现场演示。

### Stage 4：工作流工程化

先手动实现一次简单 Agent Loop，再选择 LangGraph 进行工作流重构。不要同时学习多个 Agent 框架。

本阶段任务：

- [ ] 定义工作流状态。
- [ ] 接入至少一个真实航班沙箱 API。
- [ ] 支持多个数据源并行搜索。
- [ ] 实现超时和有限次数重试。
- [ ] 单个数据源失败时仍能返回其他结果。
- [ ] 统一币种、税费、行李和退改签字段。
- [ ] 对重复行程进行去重。
- [ ] 在展示结果前重新验价。

### Stage 5：航班 MCP Server

将航班搜索能力独立封装为 MCP Server，作为简历中的第二个项目。

建议工具：

- `search_flights`
- `resolve_airport_code`
- `convert_currency`
- `verify_fare`

验收条件：

- [ ] 工具 Schema 清晰、字段有说明。
- [ ] 输入错误能返回结构化错误。
- [ ] 支持至少一个 MCP 客户端调用。
- [ ] README 包含安装和调用示例。
- [ ] 为每个工具编写基本测试。

### Stage 6：按需学习 Memory/RAG

机票 MVP 不需要完整 RAG。只在确有价值时实现用户偏好记忆，例如：

- 常用出发城市。
- 是否接受中转。
- 行李需求。
- 航司偏好。
- 常用币种。

用户偏好必须由用户明确确认，并支持查看、修改和删除。

### Stage 7：达到简历项目标准

建立至少 30-50 个固定评测场景，覆盖：

- 正常单程和往返查询。
- 缺少出发地、目的地或日期。
- 模糊日期和灵活日期。
- 多机场城市。
- 工具超时和限流。
- 部分供应商失败。
- 没有符合条件的结果。
- 币种不同。
- 行李价格缺失。
- 报价过期或重新验价失败。
- 错误机场代码。
- 用户中途修改条件。

需要实际测量：

- [ ] 参数提取正确率。
- [ ] 工具调用成功率。
- [ ] 端到端任务完成率。
- [ ] 平均响应时间及 P95 响应时间。
- [ ] 单次请求平均 Token 和模型成本。
- [ ] 失败类型及恢复成功率。

### Stage 8 和项目包装

Browser Use 仅作为没有正式 API 时的补充能力，不作为核心数据来源。

最终交付内容：

- [ ] 在线演示地址。
- [ ] 中文和英文 README。
- [ ] 系统架构图。
- [ ] 2-3 分钟演示视频或 GIF。
- [ ] 一键启动命令。
- [ ] `.env.example`。
- [ ] 示例请求和响应。
- [ ] 测试与评测报告。
- [ ] 设计决策和已知限制。
- [ ] API 不可用时的模拟数据回退方案。
- [ ] 清晰的 Commit、Issue 和 Release 记录。

## 6. 推荐技术栈

第一版应保持简单，避免同时引入太多框架。

| 层级 | 推荐选择 |
|---|---|
| 语言 | Python 3.11 或更高版本 |
| API 服务 | FastAPI |
| 数据校验 | Pydantic |
| LLM 接入 | OpenAI SDK 或兼容接口 |
| Agent 入门 | 手写简单 Tool Calling Loop |
| 工作流编排 | LangGraph |
| 测试 | pytest |
| 数据库 | MVP 先不使用，后续按需选择 PostgreSQL |
| 缓存 | MVP 先使用内存缓存，后续按需选择 Redis |
| 前端 | Streamlit 或简单 Web 前端 |
| 可观测性 | 结构化日志，加一种 Trace 工具 |
| 部署 | 选择一个支持 Python Web 服务的平台 |

## 7. 建议时间分配

```text
第 1-3 周：学习 60%，项目 40%
第 4-7 周：学习 40%，项目 60%
第 8-12 周：学习 25%，项目 75%
```

每周建议形成至少一个可验证产物：

- 一项可运行功能。
- 一组自动化测试。
- 一次架构或设计记录。
- 一次可展示的 Commit 或 Release。

## 8. 简历材料准备

简历描述应采用“问题、方案、技术、结果”的结构，并使用真实指标。

示例：

> 开发基于 Function Calling 的机票比价 Agent，将自然语言需求转换为结构化航班条件，编排多个搜索数据源，并通过确定性规则完成币种、行李及税费标准化。

> 构建包含 `[N]` 个场景的 Agent 评测集，覆盖参数缺失、工具超时、无结果及报价过期等情况，将任务完成率从 `[A]%` 提升至 `[B]%`。

> 使用 FastAPI、Pydantic 和 LangGraph 实现可追踪工作流，并记录工具成功率、响应延迟和 Token 成本。

其中 `[N]`、`[A]`、`[B]` 必须替换为项目中实际测量的结果。

## 9. 最终验收标准

项目达到以下条件后，可以作为主要简历项目：

- [ ] 新用户能根据 README 在本地成功启动。
- [ ] 用户能通过自然语言完成一次机票查询。
- [ ] Agent 不会猜测必要参数或虚构航班。
- [ ] 每条结果包含数据来源和查询时间。
- [ ] 工具超时或部分失败不会导致整个服务崩溃。
- [ ] 核心价格和排序逻辑有单元测试。
- [ ] Agent 行为有固定评测集和量化结果。
- [ ] 项目有公开演示、架构图和演示视频。
- [ ] 能在面试中解释为什么某些步骤使用 LLM，另一些使用确定性代码。

